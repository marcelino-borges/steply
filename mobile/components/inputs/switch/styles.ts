import { StyleSheet } from "react-native";

export const switchStyles = StyleSheet.create({
  root: {
    flexDirection: "row",
  },
  textContainer: {
    flexGrow: 1,
    maxWidth: "90%",
  },
  switch: {
    flexShrink: 1,
  },
});
